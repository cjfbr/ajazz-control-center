diff --git a/src/components/DeviceView.svelte b/src/components/DeviceView.svelte
index e97db8a..9b50d95 100644
--- a/src/components/DeviceView.svelte
+++ b/src/components/DeviceView.svelte
@@ -82,11 +82,11 @@

 	$: gridRowLengths = [
 		...Array(device.rows).fill(device.columns),
-		...(device.encoders > 0 ? [device.encoders] : []),
 		...(device.touchpoints > 0 ? [device.touchpoints] : []),
+		...(device.encoders > 0 ? [device.encoders] : []),
 	];
-	$: encoderRowIndex = device.rows;
-	$: touchpointRowIndex = device.rows + (device.encoders > 0 ? 1 : 0);
+	$: touchpointRowIndex = device.rows;
+	$: encoderRowIndex = device.rows + (device.touchpoints > 0 ? 1 : 0);

 	function flatIndexFromRowCol(row: number, col: number): number {
 		let index = 0;
@@ -189,6 +189,15 @@
 		on:keydown|capture={handleGridKeydown}
 		on:focusin={handleGridFocusin}
 	>
+		<!-- Keys on the left, dials in a column on their right. Stacking the
+		     dials under the grid (upstream's layout) misrepresents every device
+		     whose knobs sit beside the keys rather than below them, and on a
+		     3x3 grid it also pushed them off the bottom of a centred,
+		     overflow-auto container. -->
+		<div class="flex flex-row items-center">
+		<!-- Left column: the key grid with the touch strip (when present)
+		     beneath it, exactly as they sit on the hardware. -->
+		<div class="flex flex-col">
 		<div class="flex flex-col" role="rowgroup">
 			{#each { length: device.rows } as _, r}
 				<div class="flex flex-row" role="row">
@@ -209,22 +218,6 @@
 			{/each}
 		</div>

-		<div class="flex flex-row" role="row">
-			{#each { length: device.encoders } as _, i}
-				<Key
-					context={{ device: device.id, profile: profile.id, controller: "Encoder", position: i }}
-					bind:inslot={profile.sliders[i]}
-					on:dragover={handleDragOver}
-					on:drop={(event) => handleDrop(event, "Encoder", i)}
-					on:dragstart={(event) => handleDragStart(event, "Encoder", i)}
-					{handlePaste}
-					size={device.id.startsWith("sd-") && device.rows == 4 && device.columns == 8 ? 192 : 144}
-					label="Encoder {i + 1}"
-					tabindex={focusedRow === encoderRowIndex && focusedCol === i ? 0 : -1}
-				/>
-			{/each}
-		</div>
-
 		<div class="flex flex-row" role="row">
 			{#each { length: device.touchpoints } as _, i}
 				<Key
@@ -236,10 +229,30 @@
 					{handlePaste}
 					size={device.id.startsWith("sd-") && device.rows == 4 && device.columns == 8 ? 192 : 144}
 					isTouchPoint
+					height={device.id.startsWith("sd-") && device.rows == 4 && device.columns == 8 ? 96 : 72}
 					label="Touch point {i + 1}"
 					tabindex={focusedRow === touchpointRowIndex && focusedCol === i ? 0 : -1}
 				/>
 			{/each}
 		</div>
+		</div>
+
+		<!-- Right column: the dials. -->
+		<div class="flex flex-col justify-center" role="row">
+			{#each { length: device.encoders } as _, i}
+				<Key
+					context={{ device: device.id, profile: profile.id, controller: "Encoder", position: i }}
+					bind:inslot={profile.sliders[i]}
+					on:dragover={handleDragOver}
+					on:drop={(event) => handleDrop(event, "Encoder", i)}
+					on:dragstart={(event) => handleDragStart(event, "Encoder", i)}
+					{handlePaste}
+					size={device.id.startsWith("sd-") && device.rows == 4 && device.columns == 8 ? 192 : 144}
+					label="Encoder {i + 1}"
+					tabindex={focusedRow === encoderRowIndex && focusedCol === i ? 0 : -1}
+				/>
+			{/each}
+		</div>
+		</div>
 	</div>
 {/key}
diff --git a/src/components/Key.svelte b/src/components/Key.svelte
index 6629c34..a83b7f0 100644
--- a/src/components/Key.svelte
+++ b/src/components/Key.svelte
@@ -146,6 +146,10 @@
 	let canvas: HTMLCanvasElement;
 	let lock = new CanvasLock();
 	export let size = 144;
+	// Touch-strip segments are physically rectangular (wider than tall);
+	// when set, the canvas becomes size x height instead of a square.
+	export let height: number | null = null;
+	$: canvasHeight = height ?? size;
 	$: (async () => {
 		const sl = structuredClone(slot);
 		if (!sl) {
@@ -191,12 +195,12 @@
 	<canvas
 		bind:this={canvas}
 		class="relative border-3 border-neutral-700 rounded-3xl outline-none outline-offset-2 outline-blue-500"
-		style={`margin: ${-((size + 3 * 2 /* border */ - 132 /* desired outer size */) / 2)}px;`}
+		style={`margin: ${-((canvasHeight + 3 * 2 /* border */ - (132 * canvasHeight) / size) / 2)}px ${-((size + 3 * 2 /* border */ - 132 /* desired outer size */) / 2)}px;`}
 		class:outline-solid={active && ((slot && $inspectedInstance == slot.context) || (context && $inspectedInstance == context))}
 		class:rounded-full!={context?.controller == "Encoder"}
 		class:bg-black={slot != null}
 		width={size}
-		height={size}
+		height={canvasHeight}
 		draggable={slot != null}
 		{tabindex}
 		{role}
