# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- **`streamdock-host --list` diagnostic mode** (2026-08-21): dumps every HID interface the host
  can see as one JSON line each — VID:PID, usage page, product/serial strings, whether the SKU is
  recognised and at which protocol version, plus (on Linux) the `/dev/hidraw*` node and whether
  this process can open it read-write. That last flag is the decisive check for a missing udev
  `uaccess` rule, the most common reason a Stream Dock enumerates but does nothing.
- **Eight more AKP03 / N3 USB identities registered** (2026-08-21), all from the
  `opendeck-akp03` catalogue and previously unrecognised: `0x0300:0x1001` (AKP03),
  `0x6602:0x1000` (Mirabox N3), `0x1500:0x3001` (Soomfon Stream Controller SE), `0x0b00:0x1001`
  (Mars Gaming MSD-TWO), `0x5548:0x1001` (TreasLin N3), `0x0200:0x2000` (Redragon Skyrider
  SS-551), plus `0x0300:0x3002` as `akp03e_rev2` and `0x5548:0x6670` (Mirabox HSV293S) on the
  AKP153 side.

- **AKP05 Pro/retail SKUs registered** (2026-07-03, issue #85): `0x0300:0x3013` (AKP05E Pro),
  `0x3014` (AKP05CN Pro) and `0x3006` (AKP05 retail) are now recognised by the sidecar and the
  device registry — PIDs and protocol (v3, AKP05E image formats) mirrored from the upstream
  `opendeck-akp05` mappings. PROVISIONAL until hardware-confirmed; unlike the `0x3004` demo
  unit (whose firmware ships with input disabled — re-confirmed live 2026-07-03 with a raw
  hidraw capture: zero bytes while pressing/rotating), retail Pro units report working input
  upstream.
- **"System" builtin actions are now bindable from the editor** (2026-07-03, user report: no
  volume control, no way to rotate profiles/pages): Volume (media key up/down/mute), Media
  Control (play-pause/stop/next/previous), Next/Previous Page and Rotate Profiles now appear in
  a dedicated "System" category of the action picker with in-tree icons and Property Inspectors
  (volume.html, multimedia.html). Their executors had existed since Phase 21 but nothing ever
  advertised them to the SPA. `profile.rotate` also gained a real implementation (it only
  logged a deferral before): it cycles wrap-around through the active device's profiles —
  live-verified Default -> Streaming on the AKP05E. Existing bindings with canonical
  `com.hotspot.streamdock.*` ids are no longer mislabelled "(plugin not installed)" and now
  resolve their builtin icon + PI.
- **System Monitor sampling in milliseconds, default 100 ms** (2026-07-03, user request): the
  PI field is now "Sample every (ms)" (50-60000, default 100); the plugin renders and the
  btop-metrics-helper collects at the fastest live tile's cadence. Settings stored by older
  builds (seconds) are converted transparently. Live-verified: helper respawns with
  `--interval-ms 100`, tiles repaint at 10 Hz.

- **Manifest `Profiles[]` support — shipped-profile switching** (2026-07-03, production audit
  blocker 1): the Elgato manifest `Profiles[]` array is now parsed
  (`Name`/`DeviceType`/`Readonly`/`DontAutoSwitchWhenInstalled`/`AutoInstall`), and a
  `switchToProfile` whose token names a profile the plugin ships lazily materializes it as a
  real, user-editable profile scoped to the event's device (else the active device) and
  switches to it — enforcing the §1.4 rule that a plugin may only switch to a profile it
  ships. Bounded limitation, documented in `PRODUCTION-READINESS.md`: the bundled
  `.streamDeckProfile` layout content is not imported yet (ZIP-internal format undocumented
  in the RE corpus; no local artifact to verify against — the profile starts empty).
- **`streamdeck://` deep-link registration on Windows and macOS** (2026-07-03, production audit
  blocker 2): Windows self-registers the scheme under `HKCU\Software\Classes` at startup
  (no elevation; covers MSI and portable ZIP installs); macOS declares `CFBundleURLTypes`
  via a custom `Info.plist.in` and routes `QFileOpenEvent` activations to
  `Application::handleDeepLink`. Code-complete; runtime verification on real Windows/macOS
  hosts pending.

- **Production-readiness audit** (2026-07-03): feature-by-feature comparison against the official
  Elgato SDK docs + live UI tour with per-surface screenshot verification + packaging-artifact
  inspection. Results and the remaining blockers live in
  `docs/architecture/PRODUCTION-READINESS.md`. Fixed on the spot: per-state `setImage`/`setTitle`
  with `target` routing (blocker 3), editor now follows debug-RPC/hot-plug device activation,
  installedActions log spam de-duplicated, deb/rpm runtime dependencies declared (the published
  nightly .deb had NO Qt dependencies at all — shlibdeps cannot map aqt-provisioned Qt).
- **System Monitor: configurable sample interval** (2026-07-02): every sysmon tile's Property
  Inspector gains a "Sample every (s)" field (0.25–60 s, default 1 s). The tile's render loop
  follows its own interval; the shared btop-metrics-helper is respawned with `--interval-ms`
  set to the fastest interval across live tiles, so slowing all tiles genuinely reduces
  collection frequency. willAppear settings are now merged (not replaced) plugin-side so
  host repaint waves carrying empty settings can no longer revert a live PI edit.
  Live-verified: helper respawns 1000→5000 ms and back; tile cadence follows the setting.

### Fixed

- **AJAZZ AKP03 / AKP03E did nothing when plugged in on Linux** (2026-08-21, user report). Four
  independent defects, each on its own sufficient to make the device enumerate, appear in the
  sidebar and then respond to nothing:
  - **Wrong family for two PIDs.** `0x0300:0x1001` and `0x0300:0x1002` were registered as
    AKP153 / AKP153E. Both `4ndv/opendeck-akp03` and `4ndv/opendeck-akp153` — and this repo's
    own `docs/protocols/streamdeck/akp03.md` — put them in the AKP03 family. Because the AKP153
    is a mirajazz protocol-version-1 device, an AKP03E on `0x1002` was opened with 512-byte HID
    framing instead of 1024: every write was ignored and no input ever came back. They now
    register as `akp03` / `akp03e`, and the canonical AKP153 pairs (`0x5548:0x6674`,
    `0x0300:0x1010`) took over the `akp153` / `akp153e` codenames from the `_v1` / `_v2`
    aliases, which are gone.
  - **Protocol version and image format were per-family, not per-SKU.** `0x0300:0x3002` is the
    *rev. 2* AKP03E — protocol version 3 with 64x64 (Rot90) keys, not the pv2 / 60x60 (Rot0) of
    the original. It is now `akp03e_rev2`, and `kind.rs` carries the pv2/pv3 split for the whole
    family (`0x3003`, `0x6603:0x1002`, `0x6603:0x1003` and the four licensee rebadges are pv3).
  - **The AKP05's key and input tables were applied to every family.** Key images were uploaded
    to wire slots 10..14, past the end of the AKP03's 9 surfaces, so the panel stayed dark; the
    three plain buttons (`0x25`/`0x30`/`0x31`) and encoder 2 (`0x60`/`0x61`/`0x34`) had no
    table entry and were dropped, while encoder 0's codes (`0x90`/`0x91`/`0x33`) fired
    encoder 2. `mapAkp03Input` / `mapAkp153Input` now exist alongside the AKP05 table, selected
    from the descriptor's geometry; `displayInfo()` and `encoderInfo().hasScreens` follow the
    family too (the AKP03's knobs have no screens, and zone uploads to them are now no-ops).
  - **No udev rule for the Mirabox and rebadge vendor IDs.** Only `0x0300` was tagged, so a
    Mirabox N3/N4/HSV293S — and the AJAZZ AKP815, which is `0x5548:0x6672` — got no `uaccess`
    ACL and could not be opened at all. `70-ajazz.rules` now covers `0x5548`, `0x6602`, `0x6603`
    and, scoped to their exact product IDs, the squatted `0x1500` / `0x0b00` / `0x0200`.
- **The sidecar never enumerated three registered SKUs** (2026-08-21): `streamdock-host` kept a
  hand-maintained `KNOWN_VID_PIDS` list beside its parameter table, and the AKP05 Pro/retail
  PIDs added in the previous release went into the table only. Enumeration queries are now
  derived from the SKU table, so the two cannot drift.
- **`SidecarStreamDockDevice::open()` reported success when no device had been opened**
  (2026-08-21): it waited only for the sidecar's `ready` event, which means "enumeration
  finished", not "your device is open". A device that enumerates but cannot be opened (missing
  udev rule, wrong protocol version, handle already held) therefore looked connected while every
  command went to serial `""` and was answered with `no device`. `open()` now requires a
  `connected` event for its own VID:PID and otherwise throws with the udev remedy; each sidecar
  is additionally scoped to one device via new `--vid` / `--pid` flags, so two attached docks can
  no longer cross-wire.

- **Physical key presses ran the NEIGHBOURING key's builtin/Toggle/Multi-Action chain**
  (2026-07-03, found while cross-checking the vendor RE input conventions): the wire key index
  is 1-based (`device.hpp` contract, `akp05_input_corrections.md` §2 — confirmed against the
  official StreamDock app's `report[9]` handling) but the profile binding map is 0-based, and
  `StreamDockInputService::dispatch()` looked bindings up with the raw wire value. Plugin
  actions were unaffected (the bridge converts correctly), which is why this survived every
  plugin-focused live pass. Fixed with a single conversion at dispatch; unit tests that had
  codified the off-by-one (0-based injections) were realigned to the wire convention and a
  regression test pins it. Live-verified: wire key 7 fires the key-B2 binding, wire 6 fires B1.
- **Editor canvas could go permanently blank** (2026-07-03, found live): when the active profile
  belonged to a different device, the SPA's `get_selected_profile` non-active-device branch
  always returned null because `ProfileController::profilesForDevice` entries carried no on-disk
  `path` — the whole key grid vanished until the right profile happened to be re-activated.
  The `path` key is now included and the editor shapes the remembered profile from disk.
- **Property Inspector lost the typed text after every keystroke** (2026-07-03, user report on
  the starterpack Run Command PI): each PI `setSettings` was mirrored into the profile binding
  via `updateBindingSettings`, which emitted `profileChanged` — the SPA reloaded the profile and
  remounted the PI iframe per character. Settings-only writes now emit a narrower
  `bindingSettingsUpdated` signal that the SPA bridge does not mirror as a profile reload.
  Live-verified: a full command typed into "Key down" persists to the binding.
- **Starterpack "Device Brightness" dial never changed the panel** (2026-07-03, user report):
  the OpenDeck `deviceBrightness` event was mirror-only — it reached the SPA's
  SettingsView.svelte, a component our embed never mounts, so the hardware write upstream
  relies on never happened. The host now writes the panel directly ("set" absolute, "adjust"
  relative to the last written level, tracked per device in StreamDockControlService).
  Live-verified on the AKP05E: set 25 -> 25%, adjust +50 -> 75%.
- **Windows package defects reported in #88** (2026-07-03): three distinct root causes fixed.
  (1) The in-app version showed "0.1.0" regardless of the release — it was a hardcoded literal
  in `main.cpp`; the app version now single-sources from CMake `project(VERSION)` via the
  `AJAZZ_APP_VERSION` compile definition, and the project version itself was bumped to 2.0.0
  (it had been stuck at 0.1.1 through the v1.0/v1.1 tags). (2) The MSI created no Start Menu
  shortcut — `CPACK_PACKAGE_EXECUTABLES` + `CPACK_WIX_PROGRAM_MENU_FOLDER` were never set.
  (3) `hidapi.dll` was missing at first launch — vendored hidapi defaults to a shared build;
  it is now linked statically everywhere (`BUILD_SHARED_LIBS OFF` for the FetchContent tree),
  which also stops libhidapi/.pc artifacts leaking into the Linux deb/rpm payloads.
- **Dial actions: Switch Profile / Device Brightness now work** (2026-07-02, found live on the
  AKP05E): the starterpack's OpenDeck-extension events `switchProfile` and `deviceBrightness`
  were not in the plugin server's routed-action set and died as "unhandled event"; and on
  press-only encoder hardware (AKP05E) plugins never received `dialUp` at all — the synthesised
  encoder release only fired a test signal, so every `dial_up`-driven plugin action (profile
  switch, OBS scene commit) was a no-op. Both routed/delivered now; dialUp fires exactly once
  per press on every family. Live-verified: dial press/rotation switches the active profile,
  brightness rotation reaches the host handler, the StreamDock OBS plugin's scene dial commits
  `SetCurrentProgramScene` on press.
- **Release packaging pipeline green-up** (2026-07-02, runs 28602454635 → 28621191730): wayland/x11
  dev packages on the linux deb+rpm legs; Flatpak builder image moved to the maintained
  `ghcr.io/flathub-infra` registry with the org.kde 6.8 runtime (Qt ≥ 6.8 wayland `PRIVATE_CODE`
  codegen; the CMake side now also degrades gracefully on Qt 6.7); vendored zlib install rules
  suppressed (`SKIP_INSTALL_ALL`) so cpack no longer tries to install into `/usr/local`
  (macOS DMG EACCES / Windows WIX failure); sysmon bundle helper builds with g++ ≥ 14
  (btop needs `std::ranges::to`); packaging legs configure with `-DAJAZZ_ENABLE_WERROR=OFF`
  (a GCC 13 `-Wnull-dereference` false positive must not block shipping — the per-PR CI matrix
  keeps warnings-as-errors); per-PR CI now checks out submodules and installs g++-14 so the
  packaging build paths are exercised before merge.

### Added

- **Elgato-parity plugin UI for keys & dials + one-click install** (feature 002, 2026-06-19):
  - **Install replaces Open**: the Plugin Store row is now a single in-app action
    (Install → Installing N% → Installed / disabled "Not installable in-app"). The
    browser "Open page ↗" fallback is removed; a source without a resolvable https
    package is shown disabled with a reason, never as a browser launch.
  - **Dominant key preview**: selecting a plugin-bound key shows an Elgato-style
    140px preview (action image + bottom title overlay, live `image://livekey`),
    with a sensible placeholder when an action has no image/title.
  - **Dial owns its touch-strip segment**: on Stream Deck + class devices the
    segment above each dial mirrors that dial's bound action and a tap selects the
    dial (one control = dial + segment, the Elgato model). The independent
    touch-zone binding is retired for dial devices; legacy profiles carrying
    `touchZones` still load losslessly (read-compat).
  - Uninstalling a plugin now reverts any key/dial bound to its actions to unbound
    (no stale reference, no crash).
- **Built-in dial layouts (`$X1/$A0/$A1/$B1/$B2/$C1`) + `setFeedback`/`setFeedbackLayout`**
  (2026-06-10): the encoder feedback surface (the touch-strip zone above each dial) now renders
  the Stream Deck SDK built-in layouts — title/icon/value items, plain + gradient progress bars,
  the `$C1` dual-bar mixer row — driven by the manifest `Encoder.layout`/`Encoder.Icon` at mount
  and by `setFeedback` (item merge) / `setFeedbackLayout` (runtime switch) / `setText` (title
  alias) at runtime. Replaces the long-standing "aux-surface rendering deferred to Phase 23"
  stub. Live-verified on the AKP05E (dialRotate -> bar updates 42%->57%->72%).
- **OpenDeck/Elgato plugin-protocol parity completion** (2026-06-09/10, verified live on the AKP05E
  against a full OpenDeck source analysis): mount-time default state-image render
  (`States[i].Image` with action-`Icon` fallback painted the moment an action lands on a key —
  a plugin that never pushes `setImage`, e.g. `com.jk.weather`, now shows its icon instead of a
  blank key); host-side **automatic state cycle** for 2-state actions on `keyUp` (honours
  `DisableAutomaticStates`, paints the new state image, the `keyUp` envelope carries the new
  state, `titleParametersDidChange` follows — also after inbound `setState`); manifest-level
  `PropertyInspectorPath` now backfills actions that declare none (Elgato default-PI semantics).
- `plugin.installFromCatalog {uuid}` debug RPC driving the PluginStore tile install from the
  debug channel.

### Fixed

- **Plugin children no longer outlive the host** (`PR_SET_PDEATHSIG` on both spawn sites — the
  graceful exitApp→terminate→kill protocol lives in the destructor and never ran on
  SIGTERM/SIGKILL; 11 orphaned `node` processes had accumulated across killed sessions). A
  clean exit (code 0) of a plugin child is now logged instead of being indistinguishable from
  "never spawned".
- **Action-picker gate unified with the spawn gate**: `installedActions()` compared
  `Software.MinimumVersion` against the real app version (0.1.x) instead of the emulated
  Stream Deck version (6.9), hiding actions of plugins that were running (observed live with
  Weather); it also now mirrors the spawn step's effective-CodePath check so Windows-native
  bundles don't surface bindable-but-dead actions on Linux.
- **In-place rebind** (a different action dropped on the same key) now sends `willDisappear`
  to the old plugin, clears the lingering frame + stale title overlay, paints the new action's
  default image, and no longer inherits the old action's state index.
- Canvas live-render mirror survives profile-model rebuilds (one-shot renders such as the
  mount-time default icon vanished from the editor while staying on the device).
- CI: `qtwayland` removed from `install-qt-action` modules (the 2026-06 Qt online-repo
  restructure folded it into the base desktop install; requesting the old module name
  hard-failed all three platform legs); first cross-platform exposure of the v2.0 phases fixed
  three platform-specific failures (missing `override` under Apple Clang `-Werror`; exit-time
  `QCoreApplication` destructor SEGFAULT on Qt 6.8.3; a `processEvents`-based test pump that
  never actually waited on the windows-2022 runner).

### Changed

- UI palette polish: update banner re-skinned to the dark in-palette layer (was the only blue
  element in the dark+red scheme), encoder dial rings reserve the accent for focus/selection,
  empty-key index numbers muted.

## [0.1.1] - 2026-06-04

### Fixed

- **Windows installer now bundles the Qt runtime.** The v0.1.0 MSI/ZIP shipped only `ajazz-control-center.exe` (+ `hidapi.pc`) and the installed app failed to launch with *"The code execution cannot proceed because Qt6Widgets.dll was not found"* — the CPack WIX/ZIP generators harvest the install tree as-is, and no `windeployqt` step ran, so zero Qt DLLs/plugins/QML were packaged. A `WIN32`-gated `qt_generate_deploy_qml_app_script` + `install(SCRIPT)` now runs `windeployqt` at install time, populating `bin/` with the Qt libraries, the platform/style/imageformat/tls plugins and the QML module imports before CPack harvests it, so both the MSI and the portable ZIP are self-contained. A CI guard in `release.yml` and `nightly.yml` unzips the built package and asserts `Qt6Widgets.dll` **and** the `qwindows.dll` platform plugin are present (failing the job otherwise), so a regression in the deploy step can never again silently ship an unrunnable installer. This unblocks the winget submission, which failed manual validation on the missing runtime.

### Added

- **In-app firmware updates + device presentation** (PR #66, `research/firmware-update`). A per-device **"Firmware" tab** (`FirmwareUpdateService` QML singleton, `src/app/src/firmware_update_service.{hpp,cpp}`) shows the running firmware version and an **"Update firmware"** action that **automatically hands off to the installed vendor updater app** (`Stream Dock AJAZZ.exe` / `DeviceDriver.exe`, detected via the Windows Uninstall-registry `InstallLocation` so it survives the real install-dir names like "Stream Dock AJAZZ Global" and the CJK keyboard-driver folder), which performs the whole check→download→flash automatically — no manual firmware-file picking. We drop our HID handle first (`DeviceRegistry::closeOpenDevicesInFamily`) so the vendor flasher can claim the device. Read-only / delegate-only per `docs/architecture/FIRMWARE-UPDATES.md` — no in-app flashing, no libusb (COD-031), no firmware bundling. Presentation: each sidebar row gains a per-family device-type icon; the device editor header now shows the **product name (large) + a bundled product photo on a rounded thumbnail card + "Editing: <codename>"** (`DeviceImage.qml`, photos downsized + bundled under `resources/devices/products/`, white-bg studio shots sit on a white card). AKP05E firmware-version read corrected to the **mirajazz** `GET_FEATURE_REPORT` (report id `0x01`) method — works on Linux/macOS, a documented no-op on Windows (mirajazz #10). `0x3151:0x5007` renamed from the "2.4G 8K" placeholder to **"AJAZZ AJ159 APEX (2.4G 8K)"** per hardware confirmation. New `ADDING_A_DEVICE.md` §8.5 documents the per-family icon / product-photo / naming conventions. RE re-check corrections recorded in `docs/architecture/FIRMWARE-UPDATES*.md`.
- Clean-room original UI icon set under `resources/icons/` (+ `resources/devices/`): 24 feature glyphs, 40 key/button action-assignment icons, and 3 per-device illustrations, drawn from scratch in the brand's visual language (accent `#E63946`, dark Material surfaces) — no vendor artwork traced or reused. Line icons use `stroke="currentColor"` so one SVG renders white on the dark theme and near-black on the light theme (no per-theme duplicate assets); the brand-red highlight and the colourful `rgb`/`colorpicker`/`weather` glyphs keep explicit accents. `src/app/CMakeLists.txt` globs the folders (`CONFIGURE_DEPENDS`) and aliases each as `icons/<group>/<name>.svg`, loadable from QML via `qrc:/qt/qml/AjazzControlCenter/icons/<group>/<name>.svg`. Documented in `resources/icons/README.md` and `docs/architecture/BRANDING.md#icon-system`. (CMake configure verified.)
- Linux/hidraw device support for all three connected device families — branch `feat/linux-device-support` (`db21686`), implementing the Fedora bring-up tracked in TODO. Root cause was cross-platform HID report-id framing: hidapi's `hid_write` contract treats byte 0 of the buffer as the HID Report ID (0x00 for single-/unnumbered-report devices) on every platform, and Linux hidraw enforces this strictly, whereas Windows `WriteFile` tolerates a missing report-id byte. Two independent fixes in `src/core/src/hid_transport.cpp` plus the four streamdeck backends, both backed by the hidapi report-id contract and the platform note already in `docs/protocols/keyboard/via.md:10`:
  - **Streamdeck (AKP03/AKP05/AKP153/AKP815) image upload.** The vendor "CRT" output reports start command data at byte 0 with no report-id byte; this renders the AKP05 first-key icon on Windows but nothing on Fedora, because on hidraw byte 0 (`'C'` = 0x43) is consumed as the report number and the panel receives misaligned bytes. `makeHidTransport` gains a `prependReportIdPosix` flag (default false) that the four streamdeck device constructors set to `true`; `HidTransport::write()` then prepends a single `0x00` report-id byte **under `#ifndef _WIN32` only**, so the kernel strips it and transmits the data unshifted. The working Windows path is byte-for-byte unchanged; byte accounting reports the logical (un-prefixed) length.
  - **AJ-series mouse (VID 0x3151) interface selection.** `HidTransport::open()` now does a two-pass match: pass 1 matches `usage_page` AND `usage` (Windows reports `usage` reliably, so it picks the 0xFFFF/usage-2 control collection over the usage-1 sibling); pass 2 falls back to `usage_page`-only, because on Linux hidraw `hid_enumerate` can report `usage == 0` (unpopulated) for non-primary collections — without the fallback `open()` would drop through to `hid_open(vid,pid)` (the boot-mouse interface) and the battery (status report 0x05) + OLED clock (opcode 0x28) reads would silently fail on Fedora. The open log now reports which match kind won (`usage+page filtered` / `usage-page filtered` / `first-interface` / `default`) for one-grep Linux triage.
  - **Keyboard (AK980 PRO) needs no byte change** — its feature reports already carry a report-id byte (0x00) and the 0xFF13 control channel is a single collection (`controlUsage = 0`, matched by usage page alone), so it is already cross-platform.
  - Verified: Windows MSVC build + 365/365 unit tests pass; the Linux-only `#ifndef _WIN32` write path and the two-pass `open()` logic compile clean under `g++ -std=c++20 -Wall -Wextra` (WSL Ubuntu has only `g++`, no Qt/USB, so the full Linux build and on-device verification are performed on Fedora 44 per the TODO test plan). Lands on a branch pending that hardware confirmation.
- Empty-state polish for `DeviceList` when zero devices are online (`src/app/qml/DeviceList.qml`): the empty `ListView` is hidden and `components/EmptyState.qml` is rendered centered in the sidebar with a "No devices yet" title and a one-line onboarding hint. Suppressed in the collapsed 64 px icon-only layout (`root.width < 200`) so the title doesn't clip on narrow windows.
- Time Sync infrastructure (capability + UI + service); device-side wire format is still TBD per family. Backends honestly return `TimeSyncResult::NotImplemented` — no false success toasts (Pitfall 12 / 13 honest UX). All five backend stubs (Akp153Device, Akp03Device, Akp05Device, Akp815Device, ProprietaryKeyboard) inherit `IClockCapable` and WARN-once via per-backend `std::once_flag` (Pitfall 14). UI: per-row "Sync time" ToolButton (gated by `DeviceModel::HasClockRole`) with status glyph + tooltip; SettingsPage "Auto-sync time on device connect" toggle persisted in `QSettings("Time/AutoSync")` with Pitfall-13 load-time validation. C++ service: `TimeSyncService` QObject + QML_SINGLETON with static_assert(!is_default_constructible_v<>) Pitfall-4 build-break lock; manual-sync via `setSystemTimeOn(codename)` Q_INVOKABLE; auto-sync via `onDeviceArrivedDebounced(codename)` with 300 ms QTimer::singleShot stacked on Phase 4 D-05's hot-plug coalescing (~600 ms plug-in to fire). D-02 split: manual sync surfaces toast + glyph, auto-sync surfaces glyph only — disambiguation latch lives in QML (`timeSyncLastTrigger`). 7 unit tests + integration test exercising the full HotplugMonitor → debounce → setTime stub loop via Phase 3 ARCH-02 inject seam. (TIMESYNC-01..06; D-01..D-03; Pitfalls 2/4/13/14.) See [`docs/superpowers/specs/2026-05-13-time-sync-design.md`](docs/superpowers/specs/2026-05-13-time-sync-design.md) for the design contract and [`docs/superpowers/plans/2026-05-13-time-sync.md`](docs/superpowers/plans/2026-05-13-time-sync.md) for the implementation plan.

- A4 PluginHost out-of-process — slice 3d-ii (audit finding A4). New `ajazz::plugins::WindowsAppContainerSandbox` (`src/plugins/include/ajazz/plugins/windows_app_container_sandbox.hpp` + `src/plugins/src/windows_app_container_sandbox.cpp`) wraps the OOP child in a Windows AppContainer + restricted access token, mirroring the posture of `LinuxBwrapSandbox` and `MacosSandboxExecSandbox` on the third major desktop platform. Because AppContainer isolation is configured at `CreateProcessW` time via `STARTUPINFOEX::lpAttributeList` (not via a wrapper executable like `bwrap` / `sandbox-exec`), the `Sandbox` interface gained a second virtual method — `configureProcessAttributes(ProcessAttributes&)` — that is a no-op on POSIX and the active path on Windows. `ProcessAttributes` is a pimpl opaque type defined in `src/plugins/src/sandbox.cpp`: POSIX pimpl is empty, Windows pimpl owns the `PSID appContainerSid`, the `std::vector<SID_AND_ATTRIBUTES>` capability list (plus a parallel `std::vector<PSID>` for `FreeSid` cleanup), and the `HANDLE restrictedToken`; the destructor runs `FreeSid` on every SID and `CloseHandle` on the token, RAII-clean end-to-end. The sandbox's `configureProcessAttributes` calls `DeriveAppContainerSidFromAppContainerName` through a `userenv.dll` delay-load (so the class compiles against a bare Windows SDK and gracefully degrades to passthrough on Windows builds where AppContainer has been disabled by group policy), allocates capability SIDs via `AllocateAndInitializeSid(SECURITY_APP_PACKAGE_AUTHORITY, SECURITY_CAPABILITY_BASE_RID, rid, ...)` and produces a restricted token via `CreateRestrictedToken(primary, DISABLE_MAX_PRIVILEGE, ...)` to drop every privilege. Permission mapping mirrors the Linux / macOS rules exactly so plugin authors see one consistent model across OSes: `obs-websocket` / `spotify` / `discord-rpc` grant `SECURITY_CAPABILITY_INTERNET_CLIENT`; `notifications` / `media-control` / `system-power` additionally grant `SECURITY_CAPABILITY_INTERNET_CLIENT_SERVER` so the child can reach the Windows notification broker / MediaRemote / IOPower equivalents. The win32 host backend (`out_of_process_plugin_host_win32.cpp`) was rewritten off `_spawnvp` onto `CreateProcessW` / `CreateProcessAsUserW` with a proper STDIO pipe pair (`CreatePipe` + `SetHandleInformation` to mark the parent-end non-inheritable), and when the sandbox populated the pimpl it builds a `STARTUPINFOEXW` with `UpdateProcThreadAttribute(PROC_THREAD_ATTRIBUTE_SECURITY_CAPABILITIES)` + the `EXTENDED_STARTUPINFO_PRESENT` creation flag. Parent-end pipe HANDLEs are wrapped in CRT fds via `_open_osfhandle` so the `readLine` / `writeLine` code paths keep using the shared POSIX-shaped `_read` / `_write` helpers — only the spawn changed, IPC is unchanged. Wide command-line construction (`appendQuoted` helper) implements the inverse of `CommandLineToArgvW` so `argv[0]` in the child reads exactly what the host put in. 5 unit tests in `tests/unit/test_windows_app_container_sandbox.cpp` pin the policy surface (decorate passthrough, default container name, custom container name, permission preservation including "unknown" strings, `configureProcessAttributes` exception-safety); they run on the `windows-2022` CI slot only. E2E runtime validation on `windows-2022` (sandboxed spawn round-trip) is the remaining follow-up.

- A4 PluginHost out-of-process — slice 2.5 (audit finding A4). New `ajazz::plugins::IPluginHost` abstract base in `src/plugins/include/ajazz/plugins/i_plugin_host.hpp` is now the shared contract for both backends. `PluginHost` (legacy in-process) and `OutOfProcessPluginHost` are both `final : public IPluginHost`, with the unified method signatures: `addSearchPath(path const&) -> void`, `loadAll() -> std::size_t` (count of newly loaded plugins), `plugins() -> std::vector<PluginInfo>`, `dispatch(plugin_id, action_id, settings_json) -> bool` (false on soft failure: unknown id / handler raised; throws only on hard failures: subprocess died, interpreter crashed). Migrated `PluginHost::dispatch` from `(core::Action const&) -> void` to the unified shape — callers split the `<plugin>.<action>` id and pass the halves. `PluginInfo` moved from `plugin_host.hpp` to `i_plugin_host.hpp` (it's the value type the interface returns). `OutOfProcessPluginHost::listPlugins()` renamed to `plugins()` to match the interface. New test case `OOP plugin host: drives the full lifecycle through an IPluginHost pointer` drives the OOP backend through an `IPluginHost&` reference and verifies addSearchPath / loadAll / plugins / dispatch all dispatch virtually. Total plugin-host tests: 6/6 green; full ctest 93/93. Future callers can hold a `std::unique_ptr<IPluginHost>` slot and switch backends without source-level changes — sets up the slice-3 cleanup that deletes the in-process host.
- A4 PluginHost out-of-process — slice 2 (audit finding A4). Wire protocol extended to cover the full plugin lifecycle in the child: `add_search_path` registers a directory the next `load_all` will scan; `load_all` walks the registered paths, imports each `<dir>/plugin.py`, instantiates the `Plugin` class, and emits one `plugin_loaded` event per success / `plugin_error` per failure, terminated by a `load_complete{count}` event; `list_plugins` now returns the real loaded inventory (one `plugin{id,name,version,authors}` event per row, terminated by `plugins_complete{count}`); `dispatch{plugin_id, action_id, settings_json}` routes to the matching `Plugin.dispatch` and catches Python exceptions so a bad handler returns `dispatch_error` to the host (logged as a warning) instead of taking the child down. Each event is one flat JSON object per line — no nested arrays — so the host's mini-parser keeps covering the whole protocol without an extra JSON dependency. Two new end-to-end unit tests (`OOP plugin host: add_search_path + load_all + list_plugins discover the hello example`, `OOP plugin host: dispatch routes to the loaded handler`) drive the full lifecycle against the existing `python/ajazz_plugins/examples/hello` example, asserting the loaded metadata matches and dispatch returns `true` for known plugin/action and `false` for unknown ones (no exception). Plugin-side `print()` is redirected to the host's stderr at child startup so it cannot pollute the IPC channel. Total OOP plugin host tests: 5/5 green.
- A4 PluginHost out-of-process — slice 1 (audit finding A4). New `ajazz::plugins::OutOfProcessPluginHost` (POSIX, `src/plugins/include/ajazz/plugins/out_of_process_plugin_host.hpp` + `src/plugins/src/out_of_process_plugin_host.cpp`) spawns a child Python process via `fork()` + `execvp()` and talks to it over line-delimited JSON on a pair of pipes. Child script lives at `python/ajazz_plugins/_host_child.py` and implements the slice-1 wire protocol — `ready` handshake (sent immediately on startup with pid + python version), `list_plugins` (returns `[]` in slice 1; slice 2 will populate from the loaded `Plugin` instances), `shutdown` (graceful exit), plus a test-only `_crash_for_test` op that SIGSEGVs the child for the safety proof. Mini purpose-built JSON encoder + key extractor in the .cpp keeps the plugins library Qt-free (matches A2's "core stays Qt-free" pattern). Three new unit tests in `tests/unit/test_out_of_process_plugin_host.cpp` validate (1) spawn → list → shutdown round-trip with no zombie leak; (2) **the crash-isolation claim** — a deliberate SIGSEGV inside the child kills only the child, the host's process is unaffected, the next `listPlugins()` returns `std::runtime_error` cleanly instead of taking the whole AJAZZ Control Center process down; (3) bad-script-path rejected at construction. Lives alongside the legacy in-process `PluginHost` during the migration. Slice 2 follow-up wires `add_search_path` / `load_all` / `dispatch` through the IPC and migrates the (currently zero) callers; slice 3 adds OS-specific sandboxing (`bwrap` / `sandbox-exec` / AppContainer). Foundation is POSIX-only; Windows port deferred to slice 2.
- Vendor RE — disassembly recon of three Windows binaries (Stream Dock AJAZZ Win installer, AJ199 driver, AK820 Max RGB driver). Static analysis only via `rz-bin` (PE header + import tables + library list + section sizes + Advanced Installer string-list extraction). Tool installed (`dnf install rizin`), used, then uninstalled (`dnf remove rizin rizin-common`); binaries downloaded to `/tmp/re-recon/`, hashed, then wiped (rizin-common artefacts and `/tmp/re-recon/` both confirmed gone post-session). **No vendor instructions disassembled, no source extracted, no instructions ported into our codebase** — clean-room policy preserved (recon engineer ≠ implementer rule from `vendor-protocol-notes.md` § README). Findings documented under `vendor-protocol-notes.md` § Findings 3 & 4: (1) **the Windows Stream Dock build is Qt 5.x** (bundles `Qt5WinExtras.dll` which doesn't exist in Qt 6 — vendor is mid-migration, with macOS already on Qt 6 and Windows lagging; our open Qt 6 stack is structurally MORE modern than the vendor's Windows build); (2) **multi-process architecture**: Stream Dock ships `StreamDockAJAZZ.exe` + `SystemMonitor.exe` (system-monitor widgets) + `FirmwareUpgradeTool.exe` (separate firmware-update tool, supports the USB-CDC bootloader hypothesis from Finding 1) + `ScreenCaptureTool.exe` (separate screen-capture process) + `SplashScreen.exe` + `QtWebEngineProcess.exe`; (3) **native deps**: `libusb1.0.dll` (vendor uses libusb directly, like us), `OpenHardwareMonitorLib.dll` (sensors data source), OpenCV 4 (image processing), libwebp (WebP tile codec); (4) **15 built-in action types** enumerated from `btn_*.png` thumbnails — folder, launch, URL, hotkey, page-jump, media-eject, mouse-event, password, play-sound, switch-profile, type-text, YouTube live-chat, countdown timer, calculator, browser-back, mic mute (corroborates the QtMultimedia / new-action-types gap from Finding 1); (5) **profile bundle is `.streamDockProfile`** (not interchangeable with our `.ajazzprofile` today — opt-in import is a parity item); (6) **AJ199 + AK820 driver wrappers share a near-identical Delphi-style toolchain fingerprint** (i386, cdecl, 5-DLL Variant-API-first import order, unsigned, identical first-32 imports between two binaries from different years) — both are installer wrappers around an inner driver tool that this recon pass cannot reach without a Windows VM (deferred to a follow-up).
- Vendor RE first finding (`static-2026-04-26-streamdock-mac-001`): the Stream Dock AJAZZ macOS bundle is a **Qt 6 application**. Static analysis of the DMG layout (downloaded to ephemeral `/tmp/`, listed with `7zz l`, hashed `sha256 5e543caa…`, deleted) lists 25 Qt frameworks under `Stream Dock AJAZZ.app/Contents/Frameworks/` — including `QtWebChannel`, `QtWebEngineCore`, `QtWebEngineWidgets` (so the vendor uses the exact same Property Inspector embedding mechanism we just shipped in `pi_bridge.cpp` M1-M4), `QtWebSockets` (validates the design in `docs/architecture/PLUGIN-SDK.md` — wire format is `QWebSocketServer`-based), and surprises like `QtSerialPort` (suggests a USB-CDC bootloader handoff for firmware update — new gap), `QtMultimedia` / `QtMultimediaWidgets` (audio actions / per-button audio levels — new gap) and `QtPdf` (in-app help or PDF profile export — lower priority). No application binaries were extracted, run, or decompiled — only filesystem-level packaging metadata was read, then the working copy was wiped. Findings written up under `docs/research/vendor-protocol-notes.md` § "Vendor app architecture" with the full Qt module list and per-module implication, and the `docs/research/vendor-feature-matrix.md` gains a new "Vendor-app Qt module dependencies" section turning the dependency delta into a parity backlog.
- Vendor reverse-engineering & parity research scaffolding under `docs/research/`: four documents wire up the multi-day track filed in `e846ddb`. `vendor-software-inventory.md` catalogues 30+ official AJAZZ desktop apps with URL / size / Last-Modified / Content-MD5 (Stream Dock AJAZZ Windows + macOS, AK680/820/870 + AKS/AKP/NK keyboards, AJ199/159/179/139/039/129/358/52 + AM3 mice) — all metadata captured via HEAD probes against the vendor CDNs (Aliyun OSS + shopify + epomaker mirrors), no binaries pulled into the repo. `vendor-protocol-notes.md` ships the clean-room methodology (capture metadata table, tool checklist with explicit install + remove steps so the recon host stays clean, protocol-description shape) plus per-device-family section skeletons that fill in as USB / WebSocket / IPC captures land. `vendor-feature-matrix.md` is the gap analysis with rows seeded from `docs/_data/devices.yaml` and public feature sheets across Stream Dock keys / encoders / screens, keyboard RGB / macros / layers, mouse DPI / RGB / buttons, and cross-cutting infrastructure (single-instance, tray, autostart, themes, plugin store) — ✅ / 🟡 / ❌ / ❓ cells goal-backwards (❓ flips only when a `capture-id` confirms vendor behaviour). `vendor-techniques.md` lists 6 candidate cross-pollination techniques ranked by expected stability impact (HID reconnect debounce, firmware update retry / rollback, per-device USB chunk sizing, profile sync conflict resolution, telemetry beaconing, crash-safe persistence). `docs/research/README.md` is the index + the one place that states the clean-room and no-redistribution rules every contributor to this directory must follow.
- Settings page (`src/app/qml/SettingsPage.qml`): Material-styled controls for theme mode (Auto/Light/Dark), launch-on-login, and start-minimised. Bound to existing `ThemeService` / `AutostartService` / `TrayController` properties. Wired into `AppHeader` as a "Settings" ToolButton next to "Plugins"; opens a right-edge modal `Drawer` (360–560 px) hosting the page.
- AJAZZ wordmark visible in `AppHeader.qml`: shipped at `qrc:/qt/qml/AjazzControlCenter/branding/ajazz-logo.png`, rendered with PreserveAspectFit at 32 px height. Falls back to the product-name text label when the image fails to load (e.g. branded builds without a logo asset).
- Brand-aligned application icon: `resources/branding/app.svg` redesigned (clean-room, by Aiacos) to evoke the AJAZZ visual identity — a triangular "A" monogram with an inner droplet element rendered in the brand red (`#E63946`) on a dark rounded square. Replaces the previous 3x3 macropad-grid placeholder. Tray icon (`resources/branding/tray.svg`) updated to a matching stroked silhouette so the brand stays recognisable down to 16 px on light and dark taskbars. All raster derivatives (Windows `.ico`, macOS `.icns`, Linux hicolor PNG fallbacks, tray PNG fallbacks) regenerated from the new SVGs. README and wiki landing page now show the brand mark.
- Plugin SDK foundation: `docs/schemas/plugin_manifest.schema.json` (JSON Schema Draft 2020-12) is the authoritative manifest contract — a strict superset of the Stream Deck SDK-2 manifest, with `CodePathLin` / `CodePaths` for OpenDeck compatibility and an `Ajazz` namespace for sandbox / permissions / Sigstore signing / device-class scoping. Companion architecture document at `docs/architecture/PLUGIN-SDK.md` covers deployment shape, discovery order, lifecycle (negotiation → spawn → register → steady-state → shutdown), wire protocol (line-delimited JSON over WebSocket), per-OS sandboxing (bwrap + seccomp on Linux, `sandbox-exec` on macOS, AppContainer on Windows, `flatpak-spawn --sandbox` inside Flatpak), permission model, Sigstore supply-chain story, signed-catalogue Plugin Store and the `acc plugin scaffold/run/pack/lint` developer CLI.
- Plugin Store UI (`src/app/qml/PluginStore.qml`): Material-styled browse page mounted in a right-edge modal `Drawer` opened from the new “Plugins” button in `AppHeader`. Virtualised `GridView` of plugin tiles with name / version / author / one-line summary / colour-coded compatibility badge, install + uninstall + enable-toggle controls and a side-sheet details pane reading `pluginCatalog.entryFor()` with a revision counter that re-resolves the QVariantMap on every mutation. Source switcher tabs **All / Installed / AJAZZ Streamdock / Community** scope the catalogue, with an info banner on the Streamdock tab explaining that those plugins are mirrored from the upstream catalogue and run through the same sandboxed lifecycle as native plugins. Backed by `PluginCatalogModel` (`src/app/src/plugin_catalog_model.{hpp,cpp}`), an 11-row mock fixture exercising every compatibility mode (`native`, `opendeck`, `streamdeck`, `streamdock`) and source (`local`, `community`, `streamdock`).
- AJAZZ Streamdock store integration (compatibility mode `streamdock`): manifest schema gains `Ajazz.Compatibility.Mode = "streamdock"` plus an opaque `StreamdockProductId` resolved by the catalogue mirror to the upstream signed bundle URL, and `docs/architecture/PLUGIN-SDK.md` documents the dialect (Stream Deck SDK-2 superset with the `streamdock/*` event namespace for hardware dial widgets and per-key haptics).
- `feat(plugins)`: live Streamdock catalogue mirror with offline fallback. New `StreamdockCatalogFetcher` (`src/app/src/streamdock_catalog_fetcher.{hpp,cpp}`) walks the upstream Space catalogue (`POST https://space.key123.vip/interface/user/productInfo/list`) page by page, accumulates the plugin records, translates upstream `deviceUuid`s (`293`, `293E`, `N3`, `N4`, `D92`…) to AKP codenames (`akp153`, `akp153e`, `akp03`, `akp815`, `ak980pro`) via a curated table, and writes an atomic `QSaveFile` mirror to `<XDG_CACHE_HOME>/ajazz-control-center/streamdock-catalog.json`. Three resolution layers — live → on-disk cache → bundled `resources/streamdock-fallback.json` (compiled into the binary) — guarantee the AJAZZ Streamdock tab is never empty (first launch on an air-gapped host or CI smoke runs still get rows). `PluginCatalogModel` exposes `streamdockState` (`loading` / `online` / `cached` / `offline`) and `streamdockFetchedAtUnixMs` to QML, and `PluginStore.qml` renders a live status pill on the tab banner with a rolling relative-time label ("updated 3 min ago"). Endpoint is overridable via the `ACC_STREAMDOCK_CATALOG_URL` env var (set to `disabled` to skip the network fetch entirely on air-gapped builds). Pure helpers (`parseUpstreamJson`, `mapUpstreamDevices`, `humaniseSize`, `deriveUuid`) plus the cache round-trip and bundled fallback are covered by `tests/unit/test_streamdock_catalog_fetcher.cpp`.
- Property Inspector M1 + M2 + M3 + M4 — plugin-authored HTML Property Inspector panel embedded in the inspector pane. M1 introduced `src/app/src/property_inspector_controller.{hpp,cpp}` exposed to QML as `propertyInspectorController` (stub `loadInspector` / `closeInspector` / `webEngineAvailable` / `hasHtmlInspector` API surface plus the `AJAZZ_BUILD_PROPERTY_INSPECTOR` CMake gate). M2 wired the actual Qt WebEngine surface: `loadInspector` lazily creates a `QWebEngineProfile` per plugin UUID (so two plugins never share cookies / storage), spins up a fresh `QWebEnginePage` with a conservative `QWebEngineSettings` baseline (no remote URLs from local content, no clipboard, no fullscreen, no plugins, no screen capture), loads the HTML via `file://`, and exposes the page to QML via the new `activePage` Q_PROPERTY (forward-declared with `Q_DECLARE_OPAQUE_POINTER` so the header still compiles on minimal Qt installs without WebEngineQuick). `PropertyInspector.qml` becomes a `Loader`-based switcher between the new `PIWebView.qml` (only compiled when `AJAZZ_HAVE_WEBENGINE` is set) and the schema-driven `NativePropertyInspector.qml` (extracted verbatim from the original `PropertyInspector.qml` body so it remains the universal fallback). `main.cpp` calls `QtWebEngineQuick::initialize()` before `QApplication` when the macro is set. M3 adds the QWebChannel `$SD` JS bridge: a new `PIBridge` QObject (`src/app/src/pi_bridge.{hpp,cpp}`) ships the full Stream Deck SDK-2 PI method surface (`setSettings`, `getSettings`, `setGlobalSettings`, `getGlobalSettings`, `sendToPlugin`, `setTitle`, `setImage`, `openUrl`, `logMessage`) and signals (`didReceiveSettings`, `didReceiveGlobalSettings`, `sendToPropertyInspector`, `connected`); the controller registers it on a per-page `QWebChannel` under the name `"$SD"` before each `page->load(...)` so a PI HTML page that includes `qrc:///qtwebchannel/qwebchannel.js` and instantiates `new QWebChannel(qt.webChannelTransport, ...)` gets the same `$SD` global an Elgato Stream Deck or OpenDeck PI expects. M3 implements the bridge as a logging stub on the host side. M4 wires `setSettings` / `getSettings` / `setGlobalSettings` / `getGlobalSettings` through to atomic on-disk JSON: per-context settings land at `QStandardPaths::AppDataLocation/plugins/<pluginUuid>/settings/<contextUuid>.json` and plugin-wide settings at `QStandardPaths::AppDataLocation/plugins/<pluginUuid>/global.json` (on Linux: `~/.local/share/AjazzControlCenter/plugins/...`). Writes go through `QSaveFile` so a crash mid-write cannot corrupt the destination (write-to-temp + atomic rename, same pattern `src/core/src/profile_io.cpp` uses for profile JSON). Untrusted JS-supplied uuid components are validated against a path-traversal whitelist before being concatenated into a filesystem path: empty values, `.`, `..`, embedded slashes/backslashes, NUL or ASCII control bytes are rejected with a logged error. Per-context payloads are capped at 1 MiB and parsed via `QJsonDocument::fromJson` before the write, so a runaway plugin cannot fill the disk or stash invalid JSON. The `$SD` API surface is unchanged: getter methods stay async, returning via the existing `didReceiveSettings` / `didReceiveGlobalSettings` signals so existing PI HTML pages keep working unchanged. M5 will route `sendToPlugin` / `sendToPropertyInspector` over the plugin host's WebSocket connection.
- README + Wiki Material UI screenshots (`docs/screenshots/main-{dark,light}.png`): captured against the running app with `Appearance/Mode` preset in `QSettings`, full HiDPI 3792 × 2061 each. README hero uses a `<picture>` element with `prefers-color-scheme` so the image adapts to the visitor's GitHub theme; `docs/wiki/Home.md` shows both modes side by side under a new `## Screenshots` section. Surfaced an unrelated light-theme contrast bug (device-card delegate ignoring `themeService.mode`) — tracked under TODO → UI polish.
- App-icon helper `ajazz::app::makeAppIcon` (`src/app/src/app_icon.{hpp,cpp}`): builds a multi-resolution `QIcon` by rasterising the canonical `branding/app.svg` natively at every Qt-consumed size (16/22/24/32/48/64/128/256/512 via `QSvgRenderer`) instead of letting `QIcon` downscale a single high-res raster. Used by both the tray and the window/decoration icon path so each surface gets a sharp pixmap drawn straight from the vector; also serves as the `IconPixmap` SNI fallback for legacy hosts that do not honour `IconName` lookups.

### Fixed

- **AJ-series mouse battery readout (cross-platform, hardware-verified).** The wireless charge for the AJ159 APEX / 2.4G 8K (`0x3151:0x5007`) basetta now reads correctly on Windows (and Linux). Root cause: the basetta only mirrors the mouse's charge into vendor status report `0x05` **after** it receives the vendor's `0xF7` status poll — without it the 2.4G telemetry link stays "not ready" and every read returns `05 00 00 00` (all-zero), which is what produced the persistent `--%`. `AjSeriesMouse::batteryPercent()` now replicates the vendor `iot_driver`'s ~1 Hz heartbeat: SET_FEATURE report-id `0x00` + opcode `0xF7` (zero payload) on the `0xFFFF`/usage-`0x02` control collection, a ~30 ms settle, then GET_FEATURE report `0x05`. The charge is auto-located at byte 3 (Windows, where hidapi keeps the report-id byte at index 0) or byte 2 (Linux, unnumbered) by the shared `parseBatteryCharge()`, so one code path covers both platforms — no libusb (COD-031 preserved). Live-confirmed: the app logs `[battery] queried ajazz_24g_8k: 100%` and the sidebar chip fills in. Method captured from the vendor driver via the RE corpus (`docs/protocols/mouse/aj_series_opcode_table.md` §4); validated on hardware with `scripts/aj_battery_f7_probe.py`. Frame-validation (byte 1 zero; `05 ad 04 …` transient rejected) and the "charge 0 → grey, not 0%" rule are unchanged.

### Changed

- **Device maturity tier moved from a sidebar hover-tooltip into the per-device "Settings" tab.** The old long-hover tooltip on each `DeviceRow` (verbose, easy to miss) is gone; the Settings tab now opens with a compact colour-coded "Support maturity" chip (verified / functional / partial / probed / scaffolded) plus a one-line description. The Settings tab is now shown for every selected device (maturity applies to all). `maturity` is exposed via `DeviceModel::capabilitiesFor()`; wiring in `DeviceRow.qml` / `DeviceList.qml` / `ProfileEditor.qml` / `SettingsRow.qml`.
- **Time-sync confirmation toast now fires only for a user-initiated "Sync time now".** Automatic syncs (startup sweep, on-connect, the 15-min periodic tick, hot-plug arrivals) no longer raise a top-right green notification — they update the inline ✓ status in the device Settings tab only, restoring the D-02 "auto-sync is glyph/inline-only" design. Previously every automatic sweep emitted one toast per clock-capable device, which stacked into a burst at startup. New `TimeSyncService::manualSyncSucceeded` signal drives the toast; `syncSucceeded` still drives the inline status for both manual and automatic syncs.

### CI / automation

- `pre-commit` gains a `check-jsonschema` stage that validates every committed JSON Schema against its Draft 2020-12 metaschema and every plugin `manifest.json` against `docs/schemas/plugin_manifest.schema.json`. Manifest drift is now caught at commit time, not in CI.
- `pre-commit` clang-tidy hook now also runs at the `pre-push` stage: `make precommit` installs the matching git hook so static analysis fires once per push (catching warnings that the cheap per-commit hooks can't see), instead of waiting for CI. The hook stays cheap-on-commit because clang-tidy is not in the per-commit stage list. New `make tidy-fix` Makefile target applies clang-tidy `--fix --fix-errors` to staged C++ files for contributors who would rather have the modernize-* / readability-* checks rewrite their code than hand-fix one warning at a time. CONTRIBUTING.md gains a "Linters and the auto-fix flow" section walking through every git hook stage and what runs where.
- New weekly workflow `precommit-autoupdate.yml`: runs `pre-commit autoupdate --freeze`, re-runs every hook so the resulting auto-fixes ride the same PR, and opens a labelled pull request via `peter-evans/create-pull-request`. Combined with the existing Lint workflow's auto-fix push, the human reviewer sees only a green PR.
- `.typos.toml` now ignores image-format plural acronyms via the regex `^[A-Z]{2,5}s$` (typos was rewriting the literal sequence P‑N‑G‑s into the wrong word) and adds Plugin SDK vocabulary (Sigstore, Fulcio, Rekor, bwrap, OpenDeck, seccomp, AppContainer, QtWebEngine, WebView2, WKWebView).
- `mdformat` exclude list now covers `docs/architecture/*.md` so wide tables in the architecture docs are not reflowed into ping-pong diffs.
- Per-platform desktop / file-explorer icons. Generated from `resources/branding/app.svg`: `resources/icons/app.ico` (multi-size 16/24/32/48/64/128/256 for Windows .exe + WiX installer), `resources/icons/app.icns` (10 entries spanning 16-512 px for macOS bundle), and `resources/icons/hicolor/app-{16,24,32,48,64,128,256,512}.png` (Linux hicolor theme PNG fallbacks). Linux desktop entry installs each PNG to `share/icons/hicolor/<size>x<size>/apps/` so GNOME / KDE / GTK launchers and AppStream metadata viewers all find a crisp raster without depending on the SVG renderer.
- Tray icon refreshed: monochrome `currentColor`-based `resources/branding/tray.svg` plus white-on-transparent PNG raster fallbacks at 16/22/24/32/44/48 px under `resources/icons/tray/`. Renders correctly on light and dark menu-bars / system trays.
- Windows `.exe` now ships the application icon via an auto-generated `.rc` file (`IDI_ICON1 ICON DISCARDABLE app.ico`) so Explorer, the taskbar, and the Start Menu shortcut display the brand icon. macOS bundle is wired through `MACOSX_BUNDLE_ICON_FILE`. Runtime `QApplication::setWindowIcon()` covers Linux/X11/Wayland and serves as a fallback elsewhere.

### Changed / Hardened

- Application destructor now drains queued hot-plug events before teardown (audit finding A6, commit `0ca47c6`) so a device arriving/departing during shutdown can't dispatch into a half-destroyed `Application`.
- Removed the Apple-VID placeholder (`0x05AC:0x024F`) from keyboard registration (`src/devices/keyboard/src/register.cpp`, audit finding "Apple VID placeholder in keyboard registration"). `0x05AC` is Apple's vendor ID; the entry would have misclassified an Apple keyboard as an AJAZZ device. AK820 / AK820 Pro / AK820 Max remain reachable via the VIA entry (SONiX prefix `0x3151`). A comment block records the rationale.
- Property Inspector security hardening pass (precedes M5 plugin-host wiring): every per-plugin `QWebEngineProfile` now has a `PIUrlRequestInterceptor` installed at the same point `PropertyInspectorController::loadInspector` constructs the profile. The interceptor is scoped to the absolute Property Inspector directory (parent of `htmlAbsPath`) and gates every sub-resource fetch. Allow/deny logic lives in pure C++ at `src/app/src/pi_url_policy.{hpp,cpp}` so it is unit-testable without QtWebEngine: file:// is allowed only inside the PI dir (with `..` traversal blocked via `QDir::cleanPath`-based comparison and a trailing-slash guard against suffix-look-alike directories), https:// only to the Phase 1 CDN allowlist (`cdn.jsdelivr.net`, `unpkg.com` — pinned in a `static constexpr std::array<std::string_view, 2>` so `git grep kPiHttpsCdnAllowlist` shows the entire trust surface), qrc / blob / data are passed through for Qt internals (QWebChannel ships at `qrc:///qtwebchannel/qwebchannel.js`), and http:// + every other scheme is rejected as defence-in-depth even though `LocalContentCanAccessRemoteUrls` is already off. Each deny logs `AJAZZ_LOG_WARN("plugin-pi", ...)` with the plugin UUID, the request URL and a fixed reason tag (`scheme-blocked` / `http-blocked` / `file-outside-pi-dir` / `https-not-in-allowlist` / `malformed`) so a security review can grep the noise from one bad plugin out of the rest. `$SD.openUrl(url)` is now wired through `QDesktopServices::openUrl` after an https-only validation that rejects `javascript:`, `file:`, `mailto:`, `http:`; every call is logged at INFO with the plugin UUID + URL so audit logs see plugin-initiated browser opens. The per-plugin first-call confirmation prompt is the next step and is flagged with a grep-able `// TODO(pi-prompt)` in `src/app/src/pi_bridge.cpp`. A developer aid PI page at `resources/dev/pi/index.html` exercises the full `$SD` surface (setSettings / getSettings / setGlobalSettings / getGlobalSettings / sendToPlugin / setTitle / setImage / openUrl with allowed-and-blocked URLs / logMessage) plus listens for `connected` / `didReceiveSettings` / `didReceiveGlobalSettings` / `sendToPropertyInspector`, so the bridge can be smoke-tested manually without a live plugin host. 12 new unit tests under `tests/unit/test_pi_bridge.cpp` pin the policy contract: file:// inside-vs-outside the PI dir, https:// in-vs-not-in the allowlist (including suffix-attack hosts like `cdn.jsdelivr.net.attacker.example`), http:// blocked, qrc / blob / data allowed, exotic schemes rejected, malformed URLs rejected, the openUrl policy (https allow, javascript/file/mailto/http reject), and a pin-down test on `kPiHttpsCdnAllowlist` so adding a CDN entry is a visible CI change.
- Plugin host: rejects plugin directories whose basename collides with a Python stdlib module name (SEC-S2 hardening, uses `sys.stdlib_module_names` with a builtin-set fallback on pre-3.10) and deduplicates `sys.path` entries across repeated loads (SEC-S3). Originally implemented in the in-process `src/plugins/src/plugin_host.cpp`; when that file was retired (out-of-process host migration, slice 3e) SEC-S3 carried over but SEC-S2 was dropped — the stdlib-shadowing guard was re-ported into the OOP child `python/ajazz_plugins/_host_child.py` (`_dirname_rejection` / `_shadows_stdlib`, returns before the plugin's parent dir is prepended to `sys.path`) with regression coverage in `python/ajazz_plugins/tests/test_host_child_safety.py`.
- Profile IO (`src/core/src/profile_io.cpp`): Linux/macOS write path switched from `std::ofstream` to `::open(O_WRONLY|O_CREAT|O_EXCL|O_NOFOLLOW|O_CLOEXEC, 0600)` with `fsync()` on the original fd and `fsync()` of the parent directory after the atomic rename (SEC-S4 + SEC-S6 hardening). Eliminates symlink-redirect races and a buffered-stdio durability gap.
- Profile IO atomic rename on Windows: now uses `ReplaceFileW` for the existing-destination case (purpose-built for atomic-replace-with-backup, dodges most AV/indexer races) and falls back to `MoveFileExW` only when the target doesn't exist yet. Retry budget widened to also catch `ERROR_LOCK_VIOLATION` and `ERROR_USER_MAPPED_FILE`.
- DeviceRegistry switched from a Meyers-singleton to constructor-injected ownership (audit finding A1). `ajazz::app::Application` now holds a `core::DeviceRegistry m_deviceRegistry` data member and threads it through every `registerAll(core::DeviceRegistry&)` backend bootstrap (`streamdeck`, `keyboard`, `mouse`) plus the new `DeviceModel(registry, parent)` constructor — so per-test isolation, no hidden global state, and no startup-order foot-gun where a backend's `instance()` call fires before the Application is even constructed. The previous `DeviceRegistry::instance()` symbol is kept as a `[[deprecated]]` transition shim returning a process-wide fallback so any not-yet-migrated translation unit keeps compiling; the deprecation message embeds the audit-finding label to make grep-based migration cleanup trivial. `tests/unit/test_device_registry.cpp` was rewritten to construct its own local `DeviceRegistry` and gains a new `[registry][isolation]` case proving two locally-owned instances don't share state. All 75 existing tests still green; full build + ctest suite verified on the `dev` preset.
- Logger gains a pluggable `LogSink` abstraction (audit finding A5). `ajazz::core::LogSink` is the new abstract base; the default `StderrSink` reproduces the legacy `[<ms>] [<LEVEL>] [<module>] <message>` formatting. `setLogSink(shared_ptr<LogSink>)` swaps the active sink atomically against concurrent `log()` calls — internally an `std::atomic<std::shared_ptr<LogSink>>` so the previous sink stays alive past the swap if a `log()` call is in flight. The `AJAZZ_LOG_TRACE/DEBUG/INFO/WARN/ERROR` macros are unchanged so every existing call site keeps working unmodified; tests can now install a capturing sink and assert on what subsystems logged (`tests/unit/test_logger.cpp` ships a reusable `CapturingSink` + `ScopedSink` RAII helper plus two new cases that pin the level filter / sink dispatch contract), and a future deployment can route to journald / syslog / a Qt model without touching translation units.
- ActionEngine threading model (audit finding A2). `ActionEngine::run()` no longer calls `std::this_thread::sleep_for` on the caller — `Sleep` steps and non-zero post-step `delayMs` waits are now deferred via a pluggable `ajazz::core::Executor` so the HID poll thread / Qt main thread are freed immediately while the chain continuation runs on a worker. Core stays Qt-free: the abstraction is plain C++, with a `BlockingExecutor` default that preserves legacy in-place-sleep semantics for tests and headless callers. The GUI app ships `ajazz::app::QtExecutor` (backed by `QTimer::singleShot` with a QObject context, so destruction cancels in-flight tasks) for the production wiring path. Three new unit tests cover the contract: `BlockingExecutor` runs continuations inline, a fake async executor demonstrates that `run()` returns to its caller before the chain finishes, and post-step `delayMs` is also routed through the executor.
- EventBus refactored to copy-on-write of an immutable subscriber vector (audit finding A3). Subscribers are now held in a `std::atomic<std::shared_ptr<vector<Subscription, Handler> const>>`; `publish()` is lock-free (atomic-load + iterate the immutable snapshot), the cost shifts to the rare subscribe / unsubscribe paths which serialise on a write-only mutex and rebuild the vector once. Eliminates the per-`publish()` copy of the previous `unordered_map<Subscription, Handler>` snapshot — material on encoder-heavy devices like the AKP05 that emit >100 events/s. Existing 4/4 EventBus tests still pass; the public `subscribe` / `unsubscribe` / `publish` API surface is unchanged so all call sites stay untouched.
- System tray icon visibility on Wayland — fixed the silent invisibility under niri / Quickshell, KDE Plasma, Sway and other StatusNotifierItem-based bars. Two-part change: (1) `setDesktopFileName` now uses the simple `"ajazz-control-center"` slug that matches the file installed by the Linux CMake rule, instead of the reverse-DNS app id, so xdg-desktop-portal and Wayland tray hosts can resolve our app metadata (no more `App info not found` errors); (2) the tray + window `QIcon` is constructed via `QIcon::fromTheme("ajazz-control-center", makeAppIcon(...))` so the SNI item exposes a populated `IconName` property — most Wayland tray hosts honour only `IconName` and silently drop pixmap-only icons. The multi-size pixmap rasterised via `makeAppIcon` is kept as the embedded fallback for hosts that do honour `IconPixmap` (X11 trays, GNOME Shell + AppIndicator). Verified at runtime via `busctl --user`: SNI item now exposes `IconName='ajazz-control-center'` plus 6 ARGB pixmap entries.

- Multi-action engine (`ActionEngine`) with built-in `Plugin`, `Sleep`, `KeyPress`, `RunCommand`, `OpenUrl`, `OpenFolder`, `BackToParent` action kinds (closes #25).
- Folder navigation: nested `ProfilePage`s on AKP-class devices via `OpenFolder` / `BackToParent` (closes #28).
- Encoder bindings: dedicated `EncoderBinding` with `onCw` / `onCcw` / `onPress` chains (closes #29).
- Macro recorder scaffold with portable `MacroRecorder` interface (closes #30).
- Granular DPI editor: `IMouseCapable::setDpiStage(idx, stage)` + cached `getDpiStages()` (closes #31).
- Profile import / export as `.ajazzprofile` bundles, including `--export-profile` / `--import-profile` CLI flags (closes #32).
- Battery-aware tray tooltip + low-battery notification (closes #34).
- Cross-platform `AutostartService` with launch-on-login + `--minimized` autostart flag (closes #35).
- Cross-platform `NotificationService` (Linux notify-send back-end ships) (closes #36).
- In-app changelog and privacy viewer; embedded `CHANGELOG.md` and `docs/PRIVACY.md` (closes #37).
- `docs/schemas/property_inspector.schema.json` + generic `PropertyInspector.qml` renderer (closes #39).
- Tray menu now exposes Pause/Resume, a per-profile *Switch profile* submenu and Quit, with a live update when the profile library changes (closes #24).
- HID capture fixtures live under `tests/integration/fixtures/<device>/`; new header-only `hex_loader.hpp` parses them and `test_capture_replay.cpp` covers happy-path + malformed (truncated, oversize index, no-key marker) frames (closes #40).
- ASan/UBSan + TSan CI matrix on Linux (`Sanitizers · …` jobs in `ci.yml`); concurrent publish/subscribe and re-entrant subscribe/unsubscribe tests for `EventBus` (closes #41).

- Initial project scaffolding: CMake presets, Qt 6 / QML skeleton UI, C++20 core interfaces.
- Modular device framework with abstract `IDevice`, `ITransport`, `IProtocol` interfaces.
- Python plugin host built on pybind11 with `ajazz` runtime module.
- USB/HID transport backend based on `hidapi`.
- GitHub Actions CI matrix for Linux (Ubuntu 24.04), Windows (2022) and macOS (14, arm64).
- Release workflow producing `.deb`, `.rpm`, `.msi` and `.dmg` artifacts.
- Full documentation set: architecture, reverse-engineering methodology, device onboarding guide, plugin development guide, build guide.
- udev rules template for Linux device permissions.
- `.editorconfig`, `clang-format`, `clang-tidy`, `ruff` and `black` configurations.
- Contributor workflow: `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `SECURITY.md`, issue and PR templates.

### Changed

- **Time sync is now automatic** and lives in the device **Settings** tab. Auto-sync defaults **on** (persisted; toggle in Settings) and fires the system-time push **as soon as a device connects** (hot-plug, already wired) **and at app start** (an immediate sweep when the periodic timer first starts, not only on the 15-min tick). The per-device sync control **moved out of the sidebar row into the device "Settings" tab** (`SettingsRow.qml`, shown for any clock-capable device, ordered before "Firmware"): an "Auto-sync time on connect" switch + a "Sync time now" button with inline ✓/✗ status. The sidebar row no longer carries a sync button or status glyph; Main.qml's manual-vs-auto toast/glyph plumbing (`timeSyncLastTrigger`, `syncGlyphByCodename`) is removed in favour of the inline Settings status. Fixes a Windows-only rendering bug where the old sidebar sync ToolButton showed **two glyphs** (a themed `view-refresh` icon + the "↻" text fallback) because it set both `icon.name` and `text` — Qt resolves freedesktop icon names on Windows + Qt 6.11 but not on a minimal Wayland session, so the two platforms diverged.
- Architecture documentation refreshed to match the shipped code: clarified the snapshot-based `EventBus` synchronisation, documented the controller-layer exception bridge to QML toasts, removed the unsigned-plugin warning chip claim (deferred to #6) and added a section on the `Theme.qml` singleton (closes #44).

[Unreleased]: https://github.com/Aiacos/ajazz-control-center/compare/v0.1.1...HEAD
[0.1.1]: https://github.com/Aiacos/ajazz-control-center/compare/v0.1.0...v0.1.1
